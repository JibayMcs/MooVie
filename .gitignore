*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/
*.keystore
!debug.keystore
keystore.properties
opensubtitles.properties
/.env
/graphify-out/
/.kotlin/

# Outils de dev locaux, non versionnés (consignes projet)
/CLAUDE.md
/.claude/
/build-tools/

# Outils téléchargés pour l'empaquetage (appimagetool)
/build-tools/

# libmpv téléchargée par la CI pour les paquets Windows et macOS : un binaire
# par plateforme, versionné chez son éditeur et non chez nous.
/app/resources/

# Relevés du contrôle de santé des sources (tools/check-sources.sh) :
# des mesures, propres à une machine et à un instant — rien à versionner.
tools/reports/

# Tableau de bord des sources : le fichier porte un webhook Discord, qui est un
# secret au même titre que le keystore — quiconque l'a peut écrire dans le salon.
# tools/dashboard/config.example.json est la version publiable.
/tools/dashboard/config.json

# Bibliothèques mpv portables, déposées par tools/fetch-mpv-linux.sh
app/mpv-linux/
